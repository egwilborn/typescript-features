const today = new Date();
today.getMonth();
const person = {
  age: 20,
};
//typescript defaults to the types and properties the object has since we didn't create a new type
class Color {}
const red = new Color();
